#get user input, assign to variable "answer"
answer = input()

#assign "..." to variable "replacement"
replacement = "..."

#print user's input but replace the blank spaces with the assigned variable
print(answer.replace(" ", replacement))
